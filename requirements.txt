# Xử lý dữ liệu
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# Cấu hình & Tiện ích
pyyaml>=6.0

# Crawler (Playwright)
playwright>=1.35.0

# Hỗ trợ Async (Tùy chọn, nhưng tốt cho CrawlerManager)
aiofiles>=23.1.0